# Linux Installation

```bash
chmod +x installers/linux/*.sh
./installers/linux/install.sh
./installers/linux/start.sh
```

Open `http://127.0.0.1:8765`.

Audio playback tries `paplay`, `pw-play`, then `aplay`.

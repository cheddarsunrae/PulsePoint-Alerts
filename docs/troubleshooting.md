# Troubleshooting

## Page does not open

Confirm the app is running and browse to `http://127.0.0.1:8765`.

## Monitor running but no alerts

Check Logs. Test mode should show `Mode=TEST`; normal mode should show `Mode=UNIT`.

## Phone push silent

Check phone DND, notification channel sound, and provider settings.
